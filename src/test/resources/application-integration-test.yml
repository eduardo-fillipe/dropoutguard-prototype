spring:
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: none
    flyway:
        baseline-on-migrate: true
        locations: classpath:db/migration
    main:
        banner-mode: off

